package com.akkax.remote.models


case class UserMessage(
                        id: Long,
                        name: String,
                        date: String,
                        message: String
                      )

